# CLAUDE.md

## Project Overview

This is a website project. Stack and purpose TBD — update this section once decided.

## Development Environment

- Platform: Windows 11
- Shell: bash (Unix syntax — forward slashes, /dev/null, etc.)

## Stack / Tech Notes

> TBD — fill this in once the framework/stack is chosen (e.g., React + Vite, plain HTML/CSS/JS, etc.)

## Common Commands

> TBD — add build, dev server, and test commands here once the project is initialized.

## Conventions

- Prefer editing existing files over creating new ones
- Avoid adding dependencies unless necessary
- No speculative abstractions — only build what the task requires
- Do not add inline comments unless logic is non-obvious
- Do not add error handling for scenarios that cannot happen
